# GPR AVM Environment Configuration
# Copy to .env.avm for docker-compose

# Environment
ENVIRONMENT=development

# Database URL (PostgreSQL with PostGIS)
DATABASE_URL=postgresql://user:password@host:5432/database

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://grandprixrealty.agency

# Google Maps API Key (optional - falls back to Nominatim)
GOOGLE_MAPS_API_KEY=

# Rate limiting
RATE_LIMIT_PER_MINUTE=60

# Frontend API URL (used during build)
VITE_API_URL=http://localhost:8000
