# GPR AVM API Configuration
# Copy to .env and fill in values

# Environment: development, staging, production
ENVIRONMENT=development

# Database (PostgreSQL with PostGIS)
DATABASE_URL=postgresql://user:password@localhost:5432/gpr_avm

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://grandprixrealty.agency

# API Rate Limiting (requests per minute)
RATE_LIMIT_PER_MINUTE=60

# Google Maps API Key (for geocoding)
# Leave empty to use Nominatim (free, OSM-based) instead
GOOGLE_MAPS_API_KEY=

# ML Model path (for future use)
MODEL_PATH=models/avm_model.joblib
